apiVersion: v1
kind: Service
metadata:
  name: db-service
spec:
  clusterIP: None       # lowercase 'c'
  selector:
    app: db-service
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
